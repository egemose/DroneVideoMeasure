version: "3.3"
services:
  webapp:
    build: .
    container_name: drone-video-measure-container
    ports:
      - "5000:5000"
    volumes:
      - type: bind
        source: ./data
        target: /app/data
