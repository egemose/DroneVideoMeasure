version: "3.3"
services:
  webapp:
    build: .
    image: porpoisemeasure:latest
    container_name: porpoisemeasure-container
    ports:
      - "5000:5000"
    volumes:
      - type: bind
        source: ./data
        target: /app/data
